# 实际验证记录

日期：2026-09-20。环境：Windows / Node.js 24.16.0 / npm 11.16.0。

## 粉紫实时 3D 升级（本次）

环境：Windows，Node.js 24.16.0，内置 Chromium 浏览器；主机报告 i9-13900HX、RTX 4060 Laptop GPU（浏览器实际选用的适配器未单独核实）。本次只启动前端本地预览，后端保持关闭，因此截图中的“后端不可用”是实际状态。

### 构建和自动测试

- 改动前：9 文件 / **88 项通过**，生产构建成功。
- 改动后：`npm test` **10 文件 / 94 项全部通过**，14:45:58 执行，6.41 秒。
- 新增实际消行位置测试：连续双行、非连续行号、无消行、非法落点、原棋盘不被修改。补全 Mock/本地评分/本地基线的核心状态映射。
- 原有碰撞、候选可达性、单步、暂停/重开失效、真实返回非最佳候选执行、预算、协议路由和脱敏测试保留并通过。
- `npm run build` 成功：主 JS 389.08 KB / gzip 120.58 KB；动态加载 3D 模块 564.68 KB / gzip 143.13 KB；CSS 83.43 KB / gzip 19.53 KB。Vite 提示 3D chunk 超过 500 KB；该模块已按需加载，没有隐藏此提示。
- jsdom 无真实 Canvas/WebGL，测试日志有 getContext 未实现提示；不能把这些自动测试当作 GPU 渲染通过证明。

### 浏览器实际操作

- 首页真实入口进入 Tetris；Mock 单步产生 c009，执行后已落地 1 块，来源与模拟概率提示可见。
- 本地策略连续运行：实际观察到 **55 块、15 行、2200 分**，随后暂停；选择立体展示，查看步骤 1 的“历史快照 · tetris”与实际动作记录。
- Pathfinding：Mock 执行 down，当前位置前进一步，实际步数 1、最短路径 22、重复访问 0；展示/俯视切换正常，离开后无旧候选进入工作台。
- Playground：非法 `{invalid` 输入出现 state 的行/列错误并禁用执行；恢复合法虚构数据后单次成功，route=billing、noul=0.5、score=1.5/3 均明确为 Mock，执行次数维持 1。
- Mock 故障：请求中显示“处理中 · 等待实际结果”；随后“Mock 模拟错误 · 已使用本地兜底”，失败 1、兜底 1、c009 被执行，核心错误状态保留。验证后恢复 Mock 延迟 400ms、故障无。
- 1440×1000、1024×900、390×844 CSS 像素视口：分别测得 document scrollWidth 1425、1009、375，无横向溢出。390 下使用实验/观察台标签，控制按钮移到棋盘上方。
- 390 减少动态：有效档 reduced，检测所有 DOM 元素 animationName，非 none 数量为 **0**。静态 WebGL 仍保留几何与材质。
- 主动使用备用视图并恢复 3D 成功；此操作不是强制 GPU context loss 测试。
- 1024 供应商弹窗：动画展开时位置约 x48.65/y210.74、宽926.69/高484.44；中心保持在视口中心附近，Escape 成功关闭。未调用测试连接或修改任何模型凭据。
- 正文/次要文本对 metal 底色对比度约 **9.81:1 / 6.85:1**，依据浏览器计算的实际文本色计算；未做全站逐像素对比审计。
- 首页增强档一次实际采样：**58 FPS，CPU 提交 1.45ms，153 draw calls，76372 三角形，DPR=1**（最终场景）。它是该主机当时的采样，不是跨设备保证、GPU 耗时或模型数据。
- 浏览器日志出现过 ANGLE shader 数值精度 warning X4122（环境贴图阶段），未观察到渲染失败。未将警告屏蔽。

### 实际截图

截图均为本地应用真实运行捕获；其中实验结果仅为本地或 Mock，无付费调用。

- [桌面首页](screenshots/home-desktop.png)
- [中屏首页](screenshots/home-medium.png)
- [手机首页](screenshots/home-mobile.png)
- [Tetris 本地运行](screenshots/tetris-desktop.png)
- [Tetris 历史](screenshots/tetris-history.png)
- [手机实验区](screenshots/tetris-mobile.png)
- [手机观察台](screenshots/tetris-mobile-observer.png)
- [请求中](screenshots/tetris-request.png)
- [错误与兜底](screenshots/tetris-error.png)
- [路径俯视分析](screenshots/path-top.png)
- [减少动态](screenshots/home-reduced.png)
- [备用视图](screenshots/home-fallback.png)

### 边界

没有执行真实供应商付费请求；没有重新验证真实账号、余额和密钥。供应商后端/SQLite 未在本次改写或清空。未进行强制 context loss/恢复、真实低端设备、后台功耗或长时间 GPU 压测。当前浏览器工具未使用录制能力，未交付动效视频。

材质、灯光、质量和关键文件见 [视觉系统](VISUAL_SYSTEM.md)。下面保留此前阶段的历史验证记录，不应把旧阶段限制当成本次现状。

## 早期 CSS 3D 视觉样板

- 完成首页核心实验舱、CSS 六面立方块/沙盘/数据层微场景、行星与多深度背景，以及俄罗斯方块设备外框、倒角块、双层 ghost/目标框、落地/消行扩散。
- 增加标准/增强/减少动态档，设备提示降级、系统减少动态优先、后台动画暂停、离开视口的核心暂停。没有新增 3D 依赖或更改游戏引擎。
- `npm test`：**8 个文件，85 项全部通过**（12:19:26，7.17 秒）。新增 7 项核心实际状态映射测试，包含错误优先于兜底完成。
- 最终 `npm run build` 通过，JS 379.59 KB / gzip 116.72 KB，CSS 65.76 KB / gzip 17.10 KB。
- 浏览器实际查看桌面首页与 Tetris 截图，核心在 Mock 请求期间显示处理中，单步落地后显示结果已接收。减少动态档实际计算样式中所有轨道 animationName 为 none。
- 390×844 手机首页与 Tetris 检查无横向溢出（scrollWidth 375，innerWidth 390），验证后恢复默认视口。未做真实低性能设备 GPU 跑分或远端收费请求。路径/工作台内部完整 3D 改造尚未推广；本次样板范围为首页与 Tetris。

## Jev AI Lab 第一阶段升级

先运行原项目基线：47 项测试和生产构建全部通过。保留原测试与游戏引擎，增量重构后最终结果：

- `npm test`：**7 个文件，78 项通过，0 失败**。最终执行 12:02:50，约 6.50 秒。
- `npm run build`：全仓 TypeScript、Vite 和后端编译通过；JS 372.69 KB（gzip 114.41 KB）。
- `lab-core.test.ts`：六种种子的地图复现/可解、BFS 合法执行、障碍和终止状态、重复访问、两个工作台模板、动态题型/范围校验、跨实验状态拒绝、全部身份维度、脱敏保留数值用量、预算。
- `lab-runner.test.ts`：执行服务端非基线选择、切换实验/卸载拒绝迟到响应、暂停/重开、单次不循环、预算、连续失败与兜底、实际工作台组件拦截错误并完成 Mock。
- `api.test.ts` 新增：两种协议接受非游戏问题并保留 usage、路径由后端构造合法问题、拒绝未知实验/非法问题/不匹配答案、进行中的配置变更使响应过期。HTTP 测试使用内存 SQLite 与注入传输，不修改用户数据库。

浏览器实际检查（本次升级）：

- 桌面概览、三种工作区、真实空状态与会话记录可用；新主题截图已实际查看，右侧观察台桌面滚动时可保持可见。
- 路径种子 42 的本地 BFS 完成 **22 步**，基线最短路径 22 步，重复访问 0，终点成功；请求数为 0，未冒充模型请求。
- Tetris 桌面与 390×844 手机视口均完成一次 Mock 决策、移动、落地并自动暂停，已落地方块从 0 变 1；消行等稳定性由保留的引擎多轮测试验证。
- 工作台完成一次 Mock，执行次数 1、请求数 0；非法 state JSON 显示具体位置且禁止提交。手机观察台展示 70%/15%/15% 的明确 Mock 概率。
- 同一会话保留三个实验：路径成功 22 步、工作台成功 1 步、离开的 Tetris 已取消 1 步。查看路径历史显示“历史运行 · 非实时环境”，归属与状态正确。
- 手机“实验环境 / 决策观察台”切换正常；工作台和 Tetris 页面实际测得 scrollWidth 375、innerWidth 390，无横向溢出。验证后已恢复默认视口。
- 全局供应商弹窗显示明确内存密钥模式；Escape 可关闭，没有新增测试供应商或修改旧数据库。

限制：未使用真实 API Key，未发起真实收费请求。没有把契约测试视为远端服务联调。长期历史、批量运行、自动模型对比等第二阶段能力未实现。浏览器验证为实际交互检查，不是持久化 Playwright 自动化套件；导出脱敏由单元测试验证，浏览器未校验下载文件内容。没有推送、创建分支或部署公网，无新增临时测试文件。

以下为升级前的历史验证记录，数字不代表当前版本。

## 星空主题更新验证

- 星空背景、星云、轨道行星、玻璃质感面板及落地光波已实现；装饰动效可独立关闭，CSS 与背景 Canvas 均遵循 `prefers-reduced-motion`。
- `npm run build` 通过；更新后 `npm test` 仍为 47 项通过（约 6.19 秒）。
- 浏览器检查动态开关能切换 aria-pressed 状态；手机端单步执行后落地方块从 0 变成 1，重新开始回到 0，无浏览器控制台错误。
- 390×844 手机视口检查无横向溢出，验证后已恢复默认视口；未新增测试数据或临时文件。

## 自动化结果

- `npm test`：**5 个文件，47 项通过，0 失败**。最终一次耗时约 6.33 秒。
- `npm run build`：通过。包括全仓 TypeScript 检查、前端生产打包、后端编译。前端 JS 约 331 KB（gzip 约 100 KB）。
- 安装后依赖审计：**0 vulnerabilities**；`npm audit --omit=dev` 同样通过。初始 Vitest 审计问题已通过升级到 4.1.11 修复。
- `npm start`：在临时 3002 端口实际启动生产构建，`/api/health` 返回正常，首页 HTTP 200，包含正确项目标题。验证后关闭临时生产进程。

## 测试覆盖

| 文件 | 验证内容 |
| --- | --- |
| engine.test.ts | 7-bag、固定种子、碰撞、旋转与边界、同时消行、洞数/高度、硬降锁定、不同种子多轮中每个 BFS 候选路径可执行、本地策略 70 块运行实际消行 |
| contracts.test.ts | 三种题型、缺失答案、类型错误、无效候选、分布错误、越界与无限数值、过期身份、两种真实协议独立路由、精准 endpoint/密钥/model 传递、重试、SSRF 地址、脱敏保留错误响应 |
| store.test.ts | AES-GCM/AAD、会话内存模式、脱敏、CRUD、无密钥复制、清除密钥、测试失效、SQLite 配置跨实例持久化 |
| api.test.ts | 真实 Express HTTP 供应商 CRUD、保存前测试、测试收据随配置保存、存储凭据调用、状态版本失效、请求进行中配置变更、同源限制 |
| loop.test.ts | 暂停/重开取消、配置切换、单步落地、StrictMode 不重复发送、执行服务器返回的非最高本地分数候选、迟到远端响应不影响新局、错误来源明确的本地兜底 |

## 浏览器实际检查

通过 Codex Browser 检查本地 http://127.0.0.1:5174：

1. 默认不配置密钥，点击单步后已落地方块从 0 变为 1，并自动暂停。
2. 打开供应商表单，确认默认 OpenRouter Decisions 完整 endpoint 和模型 ID；未实现聊天协议为禁用选项。
3. 在表单添加一次性 Mock 供应商，通过后端执行实际模拟协议决策测试：成功、格式通过、耗时 18ms。没有外部费用。
4. 保存配置后列表保留连接测试结果；设为当前后刷新页面，供应商仍存在。
5. 选择后端调用模式并单步，后端返回 c009，界面实际落地 1 块；来源为 Mock，候选分布标为模拟值，最高本地方案和实际执行有独立标志。
6. 检查 390×844 手机视口：页面无横向溢出，棋盘保持 1:2 比例，供应商表单全屏；Escape 关闭成功。验证后清除视口覆盖。
7. 一次性测试供应商已删除，页面恢复默认无需密钥体验。

测试未输入任何真实 API Key。契约中的凭据均为明确的测试字符串；浏览器构建中检查不到测试凭据或真实密钥。未留下临时测试文件或测试用供应商。SQLite 运行数据和构建产物按 .gitignore 排除。

## 验证边界

**没有真实 API Key，未向 OpenRouter Decisions 或 TypeSafe 发送真实付费模型请求。** HTTP 集成使用注入的模拟传输验证请求和响应契约。真实服务的账号权限、余额、模型可用性、最新接口行为需要使用界面的连接测试确认。

不声称完成官方竞技规则认证、公网安全审计、长时间无限生存测试或模型最优性验证。
# 星轨共振增量验证（2026-09-21）

- `npm run build` 成功；保留 Three.js 懒加载块超过 500 kB 的体积提示。
- `npm test`：10 个文件、94 项通过。jsdom 提示 Canvas getContext 未实现，真实 WebGL 画面另以浏览器检查。
- 浏览器实际检查：首页线框装置渲染、珠光核心切换、手机 390×844 布局、减少动态档加载与装置切换。手机文档 scrollWidth 与 clientWidth 均为 375，没有横向溢出。
- 桌面截图：`screenshots/home-resonance.png`。参考视频仅用于本地观察，未加入网站或仓库；效果是程序化重现，并非逐帧复刻。
- 仅启动前端 5174 预览，未启动后端或调用真实模型。

## 粒子效果重建复验（2026-09-21）

- 从参考录屏采样并实际查看 48 帧及局部放大，按点云管、传播环、轨迹和符号粒子重新实现；拆解见 `VISUAL_SYSTEM.md`。
- `npm run build` 成功（3D 懒加载块 574.24 kB，gzip 146.49 kB，保留体积提示）。
- `npm test`：08:51:37 执行，10 文件 / 94 项通过，用时 6.82 秒。仍有 jsdom Canvas 环境提示。
- 实际浏览器检查：新版着色器无错误日志，装置切换正常；390×844 手机标准档页面无横向溢出，文档宽度与 scrollWidth 均为 375；关闭动态时静态呈现。
- 动态模式、增强档现场采样：57 FPS，CPU 提交 1.69 ms，87 次绘制，20,948 个三角形，DPR 1.50。此前观察到低帧率采样，不能据此保证所有环境帧率；此处仅记录恢复动态预览后的一次测量，不代表 GPU 完整耗时。
- 新截图：`screenshots/home-particles-desktop.png`、`screenshots/home-particles-mobile.png`。未修改实验规则或调用模型 API。

## 仅保留斜前方方向（2026-09-21，最新）

- 移除侧视开场与 3.93 秒切镜；首次加载、重播和时间拖动都保持斜前方，只保留小幅连续角度变化。粒子轨道与几何未改变。
- `npm test`：10:11:31 执行，11 文件 / 98 项通过，耗时 6.70 秒。方向测试覆盖起始、原切镜时刻、时间轴末尾与长时间播放；全部保持朝右下近处延伸，且没有跳变。
- `npm run build` 通过，仍有 3D 懒加载块超过 500 kB 的提示。本次没有启动前端或重新进行浏览器截图检查；下方截图与切镜说明属于此前版本。

## 圆环与参考镜头重构复验（2026-09-21，历史）

- 空心圆环、串珠光轴和三组五线谱共享连续轨道函数；主体及符号不使用爱心几何。参考 3.93 秒处为观察方向直接切换，7.17 秒之后继续斜前方运动。
- `npm test`：09:57:12 执行，11 文件 / **98 项通过**，耗时 6.62 秒。新增四项镜头测试覆盖两种视角方向、切镜时点、刚体变换及参考结束后不跳回开头。保留 jsdom Canvas 环境提示，真实 WebGL 画面另由浏览器检查。
- `npm run build` 成功：TypeScript、Vite 与后端编译通过。3D 懒加载块 574.05 kB / gzip 146.67 kB，仍有超过 500 kB 的体积提示。
- 浏览器实际检查：侧视约 2.16 秒、斜前方 7.17 秒定格；重播后可暂停并显示播放按钮；减少动态模式禁用连续播放；珠光核心与圆环切换可用，没有浏览器错误日志。
- 390×844 手机视口下文档 clientWidth 与 scrollWidth 均为 375，无横向溢出。已恢复视口与增强画质。截图：`screenshots/resonance-rings-side.png`、`screenshots/resonance-rings-front.png`、`screenshots/resonance-rings-mobile.png`。
- 此轮仅使用临时前端预览，没有启动后端或调用模型。验证结束已关闭临时标签页和前端进程，5174 无监听，关键帧拼图已清理。参考视频未加入项目；效果为程序化近似，未声称逐像素复刻。
