# Node >=22.13. Bind to loopback only; do not expose without an authenticated proxy.
PORT=3001
# 32 bytes, base64 encoded. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Missing: API keys live only in server memory; non-sensitive config still persists.
MASTER_KEY=
DATABASE_PATH=./data/jev.sqlite
